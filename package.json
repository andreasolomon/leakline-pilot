{
  "name": "leakline-payment-recovery-pilot",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22 <25"
  },
  "overrides": {
    "esbuild": "0.28.1"
  },
  "scripts": {
    "dev": "concurrently -k \"npm:dev:server\" \"npm:dev:web\"",
    "dev:web": "vite --host localhost --port 8798 --strictPort",
    "dev:server": "PORT=8797 APP_BASE_URL=http://localhost:8797 GOOGLE_REDIRECT_URI=http://localhost:8797/api/integrations/google-calendar/callback tsx watch server/index.ts",
    "build": "tsc -b && vite build && tsc -p tsconfig.server.json",
    "demo": "npm run build && node scripts/preview.mjs restart",
    "lint": "tsc -b --pretty false",
    "test": "vitest run",
    "preview": "vite preview --host localhost --port 8798 --strictPort",
    "preview:app": "node scripts/preview.mjs start",
    "preview:restart": "node scripts/preview.mjs restart",
    "preview:status": "node scripts/preview.mjs status",
    "preview:stop": "node scripts/preview.mjs stop",
    "demo:full-stats": "npm run preview:stop && npm run build && node --import tsx scripts/setup-demo-workspace.mjs && npm run preview:app",
    "start": "node server-dist/index.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.1",
    "@types/express": "^5.0.6",
    "@types/node": "^26.0.0",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@types/supertest": "^7.2.0",
    "concurrently": "^10.0.3",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "supertest": "^7.2.2",
    "tsx": "^4.22.4",
    "typescript": "latest",
    "vite": "^7.2.4",
    "vitest": "^4.1.9"
  }
}
